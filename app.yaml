runtime: python310

entrypoint: gunicorn cuidaMe.wsgi:application --workers 1 --threads 8 -b :$PORT
env_variables:
    # Tus variables de entorno aquí
  APPENGINE_URL: CuidaMe.ew.r.appspot.com
    
handlers:
  - url: /static
    static_dir: static/

  - url: /.*
    script: auto